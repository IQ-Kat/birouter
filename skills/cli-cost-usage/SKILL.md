---
name: cli-cost-usage
description: View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.

## Quick install

```bash
npm install -g birouter   # or: npx birouter
birouter --version
```

## Subcommands

### `cost`

**Flags:**

- `--period <range>`
- `--since <date>`
- `--until <date>`
- `--group-by <field>`
- `--api-key <key>`
- `--limit <n>`

**Example:**

```bash
birouter cost
```

### `pricing`

**Example:**

```bash
birouter pricing
```

### `pricing sync`

**Flags:**

- `--provider <p>`
- `--force`

**Example:**

```bash
birouter pricing sync
```

### `pricing list`

**Flags:**

- `--provider <p>`
- `--model <m>`
- `--limit <n>`

**Example:**

```bash
birouter pricing list
```

### `pricing get <model>`

**Example:**

```bash
birouter pricing get <model>
```

### `pricing defaults`

**Example:**

```bash
birouter pricing defaults
```

### `pricing show`

**Example:**

```bash
birouter pricing show
```

### `pricing set`

**Flags:**

- `--input <p>`
- `--output <p>`
- `--cache-read <p>`
- `--cache-write <p>`

**Example:**

```bash
birouter pricing set
```

### `pricing diff`

**Flags:**

- `--model <m>`

**Example:**

```bash
birouter pricing diff
```

### `usage`

**Example:**

```bash
birouter usage
```

### `usage analytics`

**Flags:**

- `--period <range>`
- `--provider <id>`

**Example:**

```bash
birouter usage analytics
```

### `usage budget`

**Example:**

```bash
birouter usage budget
```

### `usage list`

**Example:**

```bash
birouter usage list
```

### `usage get [scope]`

**Example:**

```bash
birouter usage get [scope]
```

### `usage set <amount>`

**Flags:**

- `--scope <s>`
- `--period <p>`

**Example:**

```bash
birouter usage set <amount>
```

### `usage reset [scope]`

**Example:**

```bash
birouter usage reset [scope]
```

### `usage quota`

**Flags:**

- `--provider <id>`
- `--check`

**Example:**

```bash
birouter usage quota
```

### `usage logs`

**Flags:**

- `--limit <n>`
- `--search <q>`
- `--since <ts>`
- `--follow`
- `--api-key <k>`

**Example:**

```bash
birouter usage logs
```

### `usage utilization`

**Flags:**

- `--api-key <k>`

**Example:**

```bash
birouter usage utilization
```

### `usage history`

**Flags:**

- `--limit <n>`

**Example:**

```bash
birouter usage history
```

### `usage proxy-logs`

**Flags:**

- `--limit <n>`

**Example:**

```bash
birouter usage proxy-logs
```
