---
name: cli-resilience
description: Inspect and manage circuit-breaker states, connection cooldowns, quota limits, and backoff levels from the CLI. Reset stuck providers and configure resilience thresholds.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect and manage circuit-breaker states, connection cooldowns, quota limits, and backoff levels from the CLI. Reset stuck providers and configure resilience thresholds.

## Quick install

```bash
npm install -g birouter   # or: npx birouter
birouter --version
```

## Subcommands

### `quota`

**Flags:**

- `--provider <id>`
- `--json`

**Example:**

```bash
birouter quota
```

### `resilience`

**Example:**

```bash
birouter resilience
```

### `resilience status`

**Flags:**

- `--provider <p>`

**Example:**

```bash
birouter resilience status
```

### `resilience breakers`

**Flags:**

- `--provider <p>`

**Example:**

```bash
birouter resilience breakers
```

### `resilience cooldowns`

**Flags:**

- `--provider <p>`
- `--connection-id <id>`

**Example:**

```bash
birouter resilience cooldowns
```

### `resilience lockouts`

**Flags:**

- `--provider <p>`
- `--model <m>`

**Example:**

```bash
birouter resilience lockouts
```

### `resilience reset`

**Flags:**

- `--connection-id <id>`
- `--model <m>`
- `--all-cooldowns`
- `--yes`

**Example:**

```bash
birouter resilience reset
```

### `resilience profile`

**Example:**

```bash
birouter resilience profile
```

### `resilience show`

**Example:**

```bash
birouter resilience show
```

### `resilience set`

**Example:**

```bash
birouter resilience set
```

### `resilience config`

**Example:**

```bash
birouter resilience config
```

### `resilience show`

**Example:**

```bash
birouter resilience show
```

### `resilience set`

**Flags:**

- `--threshold <n>`
- `--reset-timeout <ms>`
- `--base-cooldown <ms>`

**Example:**

```bash
birouter resilience set
```
