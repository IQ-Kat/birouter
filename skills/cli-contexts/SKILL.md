---
name: cli-contexts
description: Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.

## Quick install

```bash
npm install -g birouter   # or: npx birouter
birouter --version
```

## Subcommands

### `context-eng`

**Example:**

```bash
birouter context-eng
```

### `context-eng analytics`

**Flags:**

- `--period <p>`

**Example:**

```bash
birouter context-eng analytics
```

### `context-eng caveman`

**Example:**

```bash
birouter context-eng caveman
```

### `context-eng config`

**Example:**

```bash
birouter context-eng config
```

### `context-eng show`

**Example:**

```bash
birouter context-eng show
```

### `context-eng set`

**Flags:**

- `--aggressiveness <n>`
- `--max-shrink-pct <n>`
- `--preserve-tags <list>`

**Example:**

```bash
birouter context-eng set
```

### `context-eng rtk`

**Example:**

```bash
birouter context-eng rtk
```

### `context-eng config`

**Example:**

```bash
birouter context-eng config
```

### `context-eng show`

**Example:**

```bash
birouter context-eng show
```

### `context-eng set`

**Flags:**

- `--token-budget <n>`
- `--reserve-pct <n>`

**Example:**

```bash
birouter context-eng set
```

### `context-eng filters`

**Example:**

```bash
birouter context-eng filters
```

### `context-eng list`

**Example:**

```bash
birouter context-eng list
```

### `context-eng add`

**Flags:**

- `--priority <n>`
- `--action <a>`

**Example:**

```bash
birouter context-eng add
```

### `context-eng remove <id>`

**Flags:**

- `--yes`

**Example:**

```bash
birouter context-eng remove <id>
```

### `context-eng test`

**Example:**

```bash
birouter context-eng test
```

### `context-eng raw-output <id>`

**Example:**

```bash
birouter context-eng raw-output <id>
```

### `context-eng combos`

**Example:**

```bash
birouter context-eng combos
```

### `context-eng list`

**Example:**

```bash
birouter context-eng list
```

### `context-eng get <id>`

**Example:**

```bash
birouter context-eng get <id>
```

### `context-eng assignments <id>`

**Example:**

```bash
birouter context-eng assignments <id>
```

### `contexts`

**Example:**

```bash
birouter contexts
```

### `contexts list`

List all contexts

**Example:**

```bash
birouter contexts list
```

### `contexts add <name>`

Add a new context

**Flags:**

- `--api-key <k>`
- `--api-key-stdin`
- `--description <d>`

**Example:**

```bash
birouter contexts add <name>
```

### `contexts use <name>`

Switch active context

**Example:**

```bash
birouter contexts use <name>
```

### `contexts current`

Show current active context name

**Example:**

```bash
birouter contexts current
```

### `contexts show <name>`

Show context details

**Example:**

```bash
birouter contexts show <name>
```

### `contexts remove <name>`

Remove a context

**Flags:**

- `--yes`

**Example:**

```bash
birouter contexts remove <name>
```

### `contexts rename <old> <new>`

Rename a context

**Example:**

```bash
birouter contexts rename <old> <new>
```

### `contexts export`

Export contexts to JSON

**Flags:**

- `--out <path>`
- `--no-secrets`

**Example:**

```bash
birouter contexts export
```

### `contexts import <file>`

Import contexts from a JSON file

**Flags:**

- `--merge`

**Example:**

```bash
birouter contexts import <file>
```

### `sessions`

**Example:**

```bash
birouter sessions
```

### `sessions list`

**Flags:**

- `--user <u>`
- `--kind <k>`
- `--active`
- `--limit <n>`

**Example:**

```bash
birouter sessions list
```

### `sessions show <sessionId>`

**Example:**

```bash
birouter sessions show <sessionId>
```

### `sessions expire <sessionId>`

**Flags:**

- `--yes`

**Example:**

```bash
birouter sessions expire <sessionId>
```

### `sessions expire-all`

**Flags:**

- `--yes`

**Example:**

```bash
birouter sessions expire-all
```

### `sessions current`

**Example:**

```bash
birouter sessions current
```
