---
name: cli-routing
description: Create, list, update, and delete routing combos from the CLI. Test routing strategies, inspect combo metrics, and configure fallback chains interactively.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Create, list, update, and delete routing combos from the CLI. Test routing strategies, inspect combo metrics, and configure fallback chains interactively.

## Quick install

```bash
npm install -g birouter   # or: npx birouter
birouter --version
```

## Subcommands

### `suggest`

**Flags:**

- `--max-cost <usd>`
- `--max-latency-ms <ms>`
- `--weights <json>`
- `--top <n>`
- `--explain`
- `--switch`

**Example:**

```bash
birouter suggest
```

### `combo combo`

**Example:**

```bash
birouter combo combo
```

### `combo list`

List configured routing combos

**Flags:**

- `--json`

**Example:**

```bash
birouter combo list
```

### `combo switch <name>`

Activate a routing combo

**Example:**

```bash
birouter combo switch <name>
```

### `combo create <name>`

Create a new routing combo

**Example:**

```bash
birouter combo create <name>
```

### `combo delete <name>`

Delete a routing combo

**Flags:**

- `--yes`

**Example:**

```bash
birouter combo delete <name>
```
